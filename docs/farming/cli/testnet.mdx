---
title: Chronos Testnet
sidebar_position: 4
description: How to run an Autonomys Testnet Network Farmer with the Substrate CLI
slug: /farming/cli/testnet
sidebar_custom_props:
  testnet: true
keywords:
    - Farmer
    - Farming
    - CLI
    - Binaries
    - Docker
---
import LinuxPage from './platforms/testnet/_linux.mdx';
import WindowsPage from './platforms/testnet/_windows.mdx';
import MacOSPage from './platforms/testnet/_macos.mdx';
import SystemDPage from './platforms/testnet/_systemd.mdx';

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import TabLabel from '@site/src/components/TabLabel';
import { ICONS } from '@site/src/constants';
import Badge from '@site/src/components/Badge';

## Network Description

Chronos is our permanent testnet that started up in October of 2024. The purpose of this page is to highlight any differences between the Chronos testnet and our mainnet.

## Chain Parameter

Currently the only difference between chronos and mainnet is the chain specification (`--chain chronos` or `--chain mainnet`) and the client software. You can only run CLI, not Space Acres.

## CLI Installation Guide

:::tip Substrate Address Needed

To use this software, you need a Substrate wallet address. You can use [Talisman](/wallets/talisman) <Badge variant="recommended" text="Recommended" /> or check out other compatible [wallets](/farming-&-staking/wallets).

If you have a previous wallet address that starts with `st`, you can still use it, or you can transform it to the new `su` prefix. You can convert your existing wallet address using the tool available at [ss58.org](https://ss58.org). Previously, our testnets used the prefix `2254`, while the Chronos testnet and Autonomys mainnet use the prefix `6094`.

:::

We’ll provide the files you need for your operating system:
- **subspace-node**: The node implementation for the Subspace protocol.
- **subspace-farmer**: Manages plotting, replotting, and farming rewards.

<Tabs groupId="OS">

<TabItem value="linux" label={<TabLabel icon={ICONS.UBUNTU}>Ubuntu</TabLabel>} default>
<LinuxPage />
</TabItem>

<TabItem value="windows" label={<TabLabel icon={ICONS.WINDOWS}>Windows</TabLabel>}>
<WindowsPage />
</TabItem>

<TabItem value="macos" label={<TabLabel icon={ICONS.MACOS}>macOS</TabLabel>}>
<MacOSPage />
</TabItem>

<TabItem value="systemd_service" label={<TabLabel icon={ICONS.SERVICE}>Linux Service (SystemD)</TabLabel>}>
<SystemDPage />
</TabItem>

</Tabs>
